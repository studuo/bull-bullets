#ifndef DEF_INVENTORY
#define DEF_INVENTORY

#include <Object.h>

class Inventory {
    private:
    int size;
    
    public:
    void useObject(Object &object) {
        // Use Object
    }
    
    void checkSize(Object &object) {
        // Check if Inventory can contain new object stack14:30:32: INFO: Retag workspace completed in 0 seconds (No files were retagged)
    }
    
    void addObject(Object &object) {
        // Add Object
    }
    
    void deleteObject(Object &object) {
        // Delete Object
    }
};

#endif
