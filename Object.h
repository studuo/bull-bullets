#include <string>

class Object {
    private:
    string name;
    int stack;
    int maxStack;
    
    public:
    void checkStack(Object &object) {
        // Check if object can contain new object
    }
}